import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.raspodie.sylvestre',
  appName: 'raspodie',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
